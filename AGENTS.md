# 智能体工作守则

本指南适用于在 `tgdrive-app` 仓库中进行代码、测试与文档维护的智能体。

## 仓库边界与授权

- 本仓库维护 `shorts`、`books`、`comics`、`cinema`、`reader`、`sdk` 及独立构建、目录和发布工具。
- 宿主 `tgdrive` 是独立的私有仓库。插件任务不得修改宿主源码、配置或数据；需要了解接口时，只读查看获授权的宿主工作区或接口资料。
- 保全工作区已有改动，优先做窄范围修复，不回退用户代码、不重写 Git 历史。不确定契约或安全边界时先停止修改并联系维护者。
- 提交、推送、创建外部资源、发布 Release、部署、生成凭据等操作均需维护者明确授权，不能由代码修改任务隐式授权。
- 开发与测试不得读取生产 `.env`、连接生产数据库或操作真实网盘文件。

## 代码与文档

- 所有说明文档和新增代码注释使用中文。
- 保持 TypeScript 严格类型检查与 Vitest 回归覆盖，新增测试应断言真实行为。
- 遵循单一概念单一拼写，例如 `manifest`、`catalog`、`digest`。
- 业务插件不得依赖远程 CDN 或另一仓库的源码、依赖目录、符号链接。第三方依赖随包携带对应许可证与 NOTICE。
- 目录和发布协议以 [发布说明](docs/release.md) 为准；不能通过跳过校验、截断历史或覆盖公开资产消除错误。

## 本地独立验证

使用 Node.js `>=22.22.0`；CI 与发布验证固定使用 `22.22.0`。交付前依次执行：

```bash
npm ci
npm run check
npm test
npm run catalog verify ./catalog/catalog.json
(cd catalog && sha256sum -c SHA256SUMS)
```

`npm run check` 包含类型检查和全部插件构建。`.build/`、`catalog/`、依赖目录与测试产物不应纳入源码提交。临时文件使用调用者配置的 `TMPDIR`，不要将本机私有绝对路径或凭据写入仓库。

报告必须记录实际命令、退出结果、未执行项和剩余风险，不能将旧日志或未执行的门禁写成已通过。

## 私有宿主与本机集成

- 公开 CI 只做插件自身检查，不检出私有宿主、不配置跨仓库凭据、不上传宿主源码、构建产物或集成测试 trace。
- 跨库 E2E 仅在获授权的本机环境中使用显式 `TGDRIVE_HOST_DIR`，不猜测相邻仓库路径。执行前需完成两端构建，并确认没有其他写入者或测试占用宿主夹具端口 `4187`。
- 夹具使用临时数据库、内存 Telegram 与合成媒体；文件操作前须验证夹具身份，不能复用生产服务。
- 正式发布前必须完成本机集成验收，并由维护者将实际验证宿主的完整提交 SHA 记录到 `compatibility.json`。没有宿主权限的贡献者可独立完成上述检查，不应为公开 CI 索取宿主凭据。

更多步骤见 [开发指南](docs/development.md) 与 [贡献指南](docs/contributing.md)。
